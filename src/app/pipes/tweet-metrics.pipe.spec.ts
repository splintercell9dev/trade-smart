import { TweetMetricsPipe } from './tweet-metrics.pipe';

describe('TweetMetricsPipe', () => {
  it('create an instance', () => {
    const pipe = new TweetMetricsPipe();
    expect(pipe).toBeTruthy();
  });
});
