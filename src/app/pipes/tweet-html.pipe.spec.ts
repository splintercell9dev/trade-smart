import { TweetHtmlPipe } from './tweet-html.pipe';

describe('TweetHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new TweetHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
